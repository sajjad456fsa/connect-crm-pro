{
  "build": {
    "buildCommand": "npm install --prefix backend && npm run build --prefix backend && npm run prisma:generate --prefix backend"
  },
  "deploy": {
    "startCommand": "npx --prefix backend prisma migrate deploy && npm start --prefix backend",
    "restartPolicyType": "ON_FAILURE",
    "restartPolicyMaxRetries": 10
  }
}
