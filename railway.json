{
  "build": {
    "buildCommand": "npm install --prefix backend && npm run build --prefix backend && npx --prefix backend prisma generate"
  },
  "deploy": {
    "startCommand": "npx --prefix backend prisma migrate deploy && npm start --prefix backend",
    "healthcheckPath": "/api/health",
    "healthcheckTimeout": 300,
    "restartPolicyType": "ON_FAILURE",
    "restartPolicyMaxRetries": 10
  }
}
